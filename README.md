# manipulator
ros2_manipulator

## 真机一键启动

仓库根目录提供 `start_real_robot.sh`：

```bash
./start_real_robot.sh
```

可选参数：

```bash
./start_real_robot.sh --with-commander
./start_real_robot.sh --setup /path/to/install/setup.bash
```

日志默认输出到 `.logs/` 目录。
